{
  "name": "mcp-server-example",
  "version": "1.0.0",
  "description": "Example project for MCP server generation",
  "scripts": {
    "generate": "ts-node ./generate.ts",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "typescript",
    "mcp",
    "server"
  ],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "ts-node": "^10.9.1",
    "typescript": "^5.1.6"
  }
}